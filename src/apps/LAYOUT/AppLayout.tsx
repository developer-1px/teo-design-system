/**
 * LAYOUT App - Page Template & Section Demo
 *
 * Template 선택 → 실시간 레이아웃 미리보기 → 코드 생성
 * JetBrains IDE 스타일의 resize & drag/drop 기능 포함
 */

import { LayoutDemoPage } from './pages/layout-demo/LayoutDemoPage';

export function AppLayout() {
  return <LayoutDemoPage />;
}
