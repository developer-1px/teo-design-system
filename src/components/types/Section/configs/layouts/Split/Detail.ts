import type { RoleConfig } from '../../types';

export const Detail: RoleConfig = {
  gridArea: 'panel-b',
  overflow: 'auto',
  htmlTag: 'main',
  ariaProps: { 'aria-label': 'Detail View' },
  baseStyles: 'flex-1 flex flex-col',
};
