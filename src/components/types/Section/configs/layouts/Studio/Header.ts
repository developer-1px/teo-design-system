import type { RoleConfig } from '../../types';

export const Header: RoleConfig = {
  gridArea: 'header',
  overflow: 'hidden',
  htmlTag: 'header',
  baseStyles: 'bg-surface-elevated border-b py-2 px-4 shadow-sm',
};
