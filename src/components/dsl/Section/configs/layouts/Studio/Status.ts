import type { RoleConfig } from '../../types';

export const Status: RoleConfig = {
  gridArea: 'stat',
  overflow: 'hidden',
  htmlTag: 'footer', // Core: Status -> footer
  baseStyles: 'flex items-center px-2 min-h-[24px]',
  description: 'Status Bar (Core: Status)',
};
