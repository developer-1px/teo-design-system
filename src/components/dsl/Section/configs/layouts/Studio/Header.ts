import type { RoleConfig } from '../../types';

export const Header: RoleConfig = {
  gridArea: 'header',
  overflow: 'hidden',
  htmlTag: 'header',
  baseStyles: 'flex flex-row items-center py-2 px-4',
};
